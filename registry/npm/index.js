// Name reservation for the ltm project.
// The real CLI is a Go binary. See https://ltm-cli.dev

'use strict';

module.exports = {
  placeholder() {
    return (
      '@ltm/cli on npm is a name reservation. ' +
      'Install the real binary from https://ltm-cli.dev'
    );
  },
};
